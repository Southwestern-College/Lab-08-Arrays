/**
 * Demonstrates several common operations on integer arrays, including
 * converting an array to a string, copying an array, comparing arrays,
 * and finding the minimum and maximum values.
 * 
 * @author Your Name Here
 */
public class Arrays {

    /**
     * Returns a string representation of an integer array.
     *
     * @param arr the array to convert to a string
     * @return the array formatted as a string (for example, "[1, 2, 3]")
     */
    public static String toString(int[] arr) {
        // To-do: Implement this method
    }

    /**
     * Creates a copy of an array with the specified length.
     * If the new length is larger than the original array,
     * the remaining elements are filled with zeros.
     *
     * @param arr the array to copy
     * @param newLength the length of the new array
     * @return a new array containing the copied elements
     */
    public static int[] copyOf(int[] arr, int newLength) {
        // To-do: Implement this method
    }

    /**
     * Determines whether two arrays contain the same elements
     * in the same order.
     *
     * @param a the first array
     * @param b the second array
     * @return {@code true} if both arrays are equal; {@code false} otherwise
     */
    public static boolean equals(int[] a, int[] b) {
        // To-do: Implement this method
    }

    /**
     * Finds the largest value in an array.
     *
     * @param arr the array to search
     * @return the largest value in the array
     */
    public static int max(int[] arr) {
        // To-do: Implement this method
    }

    /**
     * Finds the smallest value in an array.
     *
     * @param arr the array to search
     * @return the smallest value in the array
     */
    public static int min(int[] arr) {
        // To-do: Implement this method
    }
}
